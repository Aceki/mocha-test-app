# mocha-test-app

Пример тестирования REST API с использованнием JavaScript фреймворка [Mocha](https://mochajs.org/).

## Запуск тестов

Установите зависимости с помощью команды `npm install`, после
чего выполните команду `npm test`.

